#############################
# Data (fully version-controlled)
#############################
# NOTE:
# - data/raw/         → TRACKED (full raw data for paper-level reproducibility)
# - data/processed/   → TRACKED (processed data / key intermediates)
# - data/metadata/    → TRACKED (FASTA + metadata)
#
# Do NOT add ignore rules for these; they are intentionally part of the repo.


#############################
# Results & logs (generated)
#############################

# All generated results (alignments, trees, designs, distances, figures, etc.)
# These can always be regenerated from data + scripts.
results/*
# But keep a small README to explain the structure of results/
!results/README.md

# Pipeline & SLURM logs — users get their own when they run the pipeline
logs/
slurm-*.out
slurm-*.err

# CD-HIT cluster log files
*.clstr


#############################
# Environments & caches
#############################

# Conda env directory (we only track env/environment.yml)
env/*
!env/environment.yml

# Local virtualenvs / env files
.venv/
.env/

# Python caches (in case you add Python utilities)
__pycache__/
*.py[cod]
.ipynb_checkpoints/


#############################
# OS / editor cruft
#############################

.DS_Store
Thumbs.db
*.swp
*.swo
